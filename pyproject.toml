[project]
name = "research-workspace"
version = "0.1.0"
description = "Research workspace for experiments"
requires-python = ">=3.10"
dependencies = [
    "arxiv>=4.0.0",
    "httpx>=0.28.1",
    "pypdf>=6.14.2",
    "requests>=2.33.1",
]

[tool.uv]
package = false
